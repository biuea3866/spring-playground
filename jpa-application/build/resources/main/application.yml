spring:
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  datasource:
    # url: "jdbc:h2:tcp://localhost:8082"
    url: "jdbc:h2:~/test"
    username: sa
    driver-class-name: org.h2.Driver